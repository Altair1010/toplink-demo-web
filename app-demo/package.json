{
  "name": "y-vien-toplink-demo",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "format:check": "prettier --check .",
    "check:tokens": "node scripts/check-tokens.mjs",
    "check:release": "node scripts/check-release-surface.mjs",
    "verify": "npm run build && npm run typecheck && npm run format:check && npm run check:tokens && npm run check:release"
  },
  "dependencies": {
    "next": "^16.2.9",
    "react": "^19.2.7",
    "react-dom": "^19.2.7"
  },
  "devDependencies": {
    "@playwright/cli": "^0.1.18",
    "@tailwindcss/postcss": "^4.0.0",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "prettier": "^3.9.4",
    "tailwindcss": "^4.0.0",
    "typescript": "^5"
  },
  "engines": {
    "node": ">=20"
  }
}
