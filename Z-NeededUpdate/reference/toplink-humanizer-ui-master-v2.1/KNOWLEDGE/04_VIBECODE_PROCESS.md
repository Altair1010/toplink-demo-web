# Engineering / Process Evidence — Vibecode Corpus

The supplied research corpus argues that commercial interface work should be represented as a graph rather than a screenshot-to-code loop:

`business intent → user/job → task/state graph → references/data → design system → prototype → implementation → verification → real users`

It also introduces human gates and decision provenance to stop infinite subjective edit loops.  
**Nguồn nội bộ:** `Pasted markdown(3).md` · SHA-256 `409f7d41f8e6f44939b3395d351aeda9b1e8481b81a2deafa2c04f28c700553b` · Sections II, VIII, XIV–XVII

## Humanizer adaptation

`evidence → user reality → narrative/state → reference decomposition → grayscale wireframe → 3 directions → Gate A → grammar → Gate B → migration → implementation → verification → Gate C`

External tools accelerate edges in this graph; they do not become design authority.
