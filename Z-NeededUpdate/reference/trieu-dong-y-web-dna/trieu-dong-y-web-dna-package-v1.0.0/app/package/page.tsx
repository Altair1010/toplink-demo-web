import capture from "@/docs/web-dna/02_evidence/capture-summary.json";
import tokens from "@/docs/web-dna/04_ui_ir/design-tokens.json";
import Link from "next/link";
import { Eyebrow, PageShell } from "../components/Shell";
const graphs=[["Site Graph","634 nodes · 39.038 edges"],["Asset Graph","1.939 resource nodes"],["Composition Graph","21 archetype + component grammar"],["Interaction Graph","Navigation · search · lookup · intent"],["Motion Graph","Computed transitions · reduced-motion"],["Decision Graph","Evidence → hypothesis → verification"]];
export default function PackagePage(){return <PageShell><section className="package-hero"><Eyebrow>Codex handoff · Web DNA v0.8</Eyebrow><h1>Một package biết<br/><em>mình đến từ đâu.</em></h1><p>Raw evidence, normalized content, graph, tokens, mindset, task contracts và ứng dụng tham chiếu được giữ cùng provenance.</p><div className="package-metrics"><span><strong>{capture.routes.captured}</strong>routes captured</span><span><strong>{capture.evidence.unique_asset_references}</strong>asset references</span><span><strong>{capture.evidence.link_edges}</strong>link edges</span></div></section><section className="graph-grid">{graphs.map(([name,detail],i)=><article key={name}><span>G{String(i+1).padStart(2,"0")}</span><h2>{name}</h2><p>{detail}</p></article>)}</section><section className="token-section"><div><Eyebrow>Evolved semantic layer</Eyebrow><h2>Token có lý do,<br/>không chỉ có giá trị.</h2></div><div className="swatches">{Object.entries(tokens.evolved.color).map(([name,value])=><div key={name}><i style={{background:value}}/><span>{name}</span><code>{value}</code></div>)}</div></section><section className="package-cta"><h2>So sánh hai lớp.</h2><p>Baseline giữ content/provenance; evolved app thử nghiệm dual journey và UI mindset mới.</p><Link href="/baseline">Mở baseline →</Link><Link href="/">Mở evolved home →</Link></section></PageShell>}

