export {
  parseArticle as normalizeArticle,
  parseMedia as normalizeMedia,
  parseProduct as normalizeProduct,
  parseService as normalizeService,
  parseSiteSettings as normalizeSiteSettings,
} from "@/lib/cms/schemas";
