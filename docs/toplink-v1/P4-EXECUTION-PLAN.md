# P4 Execution Plan — Fixture-driven Frontend

**Base:** `f3bcca8c31a21eed6ab0505e9d740bbe36fa157b`  
**Branch:** `feat/v1-frontend-fixtures`  
**Target:** independent `web/`; preserve `app-demo/` unchanged.

## Route

1. Recover approved P3B authority and verify the clean linked worktree.
2. Create the portable Next.js foundation and exact five-domain TypeScript boundary.
3. Add visibly bounded `REFERENCE_ONLY` fixtures behind `lib/content/`.
4. Implement Direction C tokens, typography, motion, shell and structural primitives.
5. Prove home, service-detail and knowledge-detail morphology first.
6. Expand all 12 P2 route patterns without WordPress or production facts.
7. Run 12 × 4 browser geometry/state checks, perceptual review, one root-cause correction batch and
   one blocker confirmation.
8. Record structural/craft evidence, verify repository boundaries, push and stop before P5.

## Invariants

- No `app-demo/` mutation, file deletion, WordPress, deployment or merge.
- Missing contacts/media/operational facts fail closed.
- Components consume Toplink domain data, not fixture modules or WordPress shapes.
- Direction C morphology remains authoritative; conditional assets remain replaceable slots.
