# Codex Boot Prompt

Use this as the first task in a fresh Codex session:

> Open the repository `Altair1010/toplink-demo-web` from the local workspace. Do not mutate anything yet. Read `AGENTS.md` and, if installed, every file under `docs/toplink-v1/` in the order stated by `START-HERE.md`. Then inspect git status, current branch, HEAD SHA, package.json, Next config, workflows, app/components/styles/scripts tree and any live WORKSTATE file. Report: (1) current phase, (2) current branch/HEAD, (3) drift from the master package, (4) blockers, (5) the single next bounded implementation step. Do not merge, deploy, change DNS, change repo visibility, or introduce a dependency unless the active phase and decision ledger authorize it. Preserve factual/evidence boundaries; PENDING is not permission to invent. Hallmark handles structural variety; Impeccable handles design context/runtime/QA; Toplink docs are the final authority.
