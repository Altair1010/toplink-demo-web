# P4 Frontend Handoff — Locked Direction C

## Implementation order

1. Create role-based token layers from `TOKENS.md`, type contract and reduced-motion foundation.
2. Build structural primitives: gateway, court, chamber, threshold, reading hall and release.
3. Implement the homepage as the visual/morphology anchor using the P3B lock evidence.
4. Implement service detail to prove purpose/process/limits/contact ordering.
5. Implement knowledge detail to prove independent long-form completion without sales pressure.
6. Validate 375/768/1280/1440, keyboard focus, reduced motion and content omission.
7. Expand remaining routes through `PAGE-MORPHOLOGY-LIBRARY.md`, not a universal page template.

## Visual authority

- Primary browser authority: `p3b/EVIDENCE-INDEX.md` and `p3b/evidence/`.
- System authority: `DESIGN.md`, `TOKENS.md`, `TYPE-SYSTEM.md`, `MOTION.md`, morphology and structure memory.
- P3A C screenshots remain provenance, not the final lock when they differ from P3B evidence.
- `app-demo/**` and H7 remain engineering salvage only; they have no target-design authority.

## Critical invariants

- Pure C structural world; no informal A/B hybrid.
- Product truth, routes and content contracts remain P2-owned.
- Limitations/evidence are readable before optional contact.
- Knowledge detail can finish without contact.
- Pending contact/media/assets fail closed.
- No generic card-grid collapse, literal palace ornament or repeated section reveal.
- CSS/native motion first; reduced-motion is a complete final state.

## Conditional asset slots

P4 may implement explicit slots for `logo.*`, `ornament.asset`, `identity.anchor`, `identity.metal`,
`material.plane` and `media.treatment`. Use the documented conditional defaults only in non-production
scaffolds. If real assets remain missing, preserve text identity, omit evidence-dependent media/routes
and keep morphology stable. Never substitute Sen/H7 assets or fabricate operational truth.

## Anchor-surface completion test

An implementer succeeds when home, service detail and knowledge detail remain visibly different with
all images removed; every token maps to a documented role; and the four missing input categories can
be inserted without rewriting layout, hierarchy or content order.
