# P9 Open Findings

## Blocking

`BLOCKED_INFRA_INPUT`: Provide SSH access to the VPS to be used for Toplink.

No public headless URL, CMS host, persistent production volume, backup artifact or target runtime exists in the accessible environment. Therefore `PUBLIC_ONLINE` and local-machine independence cannot be claimed.

## Non-blocking deferred truth

Hotline, Zalo, Facebook/Messenger, address, hours, legal identifiers, real service/product content, authorized premises/media and customer stories remain absent by design. Their absence must produce zero actionable control and truthful held states.

## Operational findings requiring target evidence

- `toplink.vn` already serves a different WordPress/LiteSpeed site behind Cloudflare. Do not cut it over or replace it without understanding ownership, DNS access and rollback.
- `cms.toplink.vn` and `www.toplink.vn` do not currently resolve.
- Docker image runtime, Caddy validation, firewall, disks, volumes, auto-start, logs, monitoring activation, certificate issuance, webhook, preview, outage, restart, backup and restore remain unexecuted.
- Prepared systemd monitoring exposes failures through service state/journal, but an external notification destination must use the selected host/provider capability; none is available yet.
- Real Safari, physical-device and assistive-technology review remain `HUMAN DEVICE CHECK PENDING` and do not block future `PUBLIC_ONLINE` under the current instruction.
