# Phase Card P0 — Foundation

## Entry

- User decisions in this package approved.
- Current repo readable.

## Tasks

1. Verify `main` SHA and clean worktree.
2. Record current deploy URL and screenshot baseline.
3. Create pre-V1 archive/tag after user gate.
4. Prepare repo-private transition; verify Pages consequence.
5. Create branch `chore/v1-foundation`.
6. Add `docs/toplink-v1/` package docs.
7. Create live `WORKSTATE.md`.
8. Update `AGENTS.md` on branch so H7 design rules are no longer target authority.
9. Inventory repo components/dependencies.
10. Propose `app-demo` → `web` restructure, but do not mix with visual implementation in the same commit unless the diff remains easily reviewable.

## Exit evidence

- no production design changed yet;
- archive/rollback clear;
- docs tracked;
- branch/PR created;
- user approves next phase.
