# 01 — Gate B-R Decision and Migration Gate-R Scope

- **Planning baseline:** `5afa11f27c8a5f54465cc6ed73bdad47c14c158b`
- **Branch / upstream:** `main` / `origin/main`
- **Entry worktree:** clean
- **Target authority:** `DEC-H5R-GBR-01 — APPROVED / LOCKED TARGET SUBTREE`
- **Runtime authority:** actual `app-demo` source
- **Mutation authority:** none; documentation only
- **Migration:** frozen
- **H6:** inactive

## Two truths

The locked design contract answers what Toplink should become. Current source, current tokens and
legacy `DESIGN.md` answer what runs now. Gate B-R does not cross that boundary. Migration Gate-R
defines reviewable, independently reversible deltas; only a later human approval may activate a
named batch.

## Rebuild frontier

Historical `docs/humanizer/migration/**` remains the pre-H5.5 plan and historical provenance. It is
superseded for implementation planning by `docs/humanizer/migration-r/**`. This rebuild re-derives:

1. M0R semantic foundation from the corrected 41 active conceptual roles;
2. M1 sans-first migration from fresh Noto consumers;
3. M2A orientation core and M2B narrative completion;
4. M3S sanitation separately from M3P evidence population;
5. M4 transport as a separately blocked operational/architecture decision;
6. M5 cleanup only after zero-consumer proof.

## Evidence-later boundary

```text
DESIGN STRUCTURE ------------------------> MAY BE ELIGIBLE
       |
       +--> evidence approved ----------> CONTEXTUAL INSERTION
       `--> evidence absent ------------> TRUE COLLAPSE

FACTUAL POPULATION
       |
       +--> DE-01..04 discharged --------> REVIEW FOR PUBLICATION
       `--> debt still open ------------> DEFER / OMIT
```

Evidence deferral is not permission to publish mock content. Structure and factual population are
tracked independently in artifact 04.

## Lane and architecture constraints

- Sen validates only purpose → orientation → place/people → scope/service → visit expectation →
  action. It contributes no palette, type, geometry, motif or image treatment.
- Triều validates only terminology, learning relationships and source/review discipline. It
  contributes no visual grammar or Toplink facts.
- Static export, basePath ownership, GSAP-only advanced motion, native scroll, reduced motion,
  accessibility, security/privacy, token hygiene and `npm run verify` remain hard invariants.
- No CMS, backend, transport provider, dependency or deployment change is proposed.

## Gate consequence

This artifact records a locked target and a planning scope, not an implementation approval. The
next gate is **HUMAN MIGRATION APPROVAL** for one or more exact READY batches. Until then, no M0R–M5
batch and no H6 work is active.
