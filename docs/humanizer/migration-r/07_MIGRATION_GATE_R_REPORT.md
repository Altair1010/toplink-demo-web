# 07 — Migration Gate-R Report

## Status

**MIGRATION GATE-R STATUS: PASS — PROPOSED FOR HUMAN MIGRATION APPROVAL**

- Gate B-R: `APPROVED — DEC-H5R-GBR-01`
- Corrected target: `LOCKED`
- Migration authority: none until the human names a READY batch
- Architecture change: none
- Migration: frozen
- H6: inactive

## Fresh runtime inventory

Fresh inspection at `5afa11f…` found:

- 45 explicit Noto/display sites and 10 implicit h1/h2 sites: **55 sites in 26 TSX files**;
- one homepage route backed by 10 current section/state components, shared recommendation logic,
  mock content and dedicated CSS;
- a direct unsafe graph from symptoms to Tắc/Hàn/Hư/Loạn and ranked services;
- **four JSON-LD emitters**, ten metadata surfaces and 11 rendered mock-claim groups;
- a ten-path booking boundary with placeholder Google Form IDs and unproven success;
- Be Vietnam Pro already loaded, two dormant seasonal skins and the existing GSAP/native-scroll/
  reduced-motion engineering substrate.

No production source changed during this inventory.

## Corrected target and batch results

### M0R — CONDITIONAL

- 41 conceptual active roles classified as: 13 global-alias candidates, 8 existing type/scale aliases, 3
  component-local roles, 8 CSS class/state roles and 9 documentation-only contracts.
- Exact global aliases proposed for immediate M0R: **0**; exact calibrated candidate pool: **13**.
- Existing token/scale roles reused without new global aliases: **8**.
- Component-local roles: **3**.
- CSS state + documentation-only roles: **17**.
- `action.danger`, `state.pending` and `state.confirmed-future` remain private/deferred outside the
  active 41 public set.
- One exact candidate owner: `app-demo/styles/tokens.css`.
- M0R aliases/consumers now: 0/0; immediate visual delta: 0.
- Current crimson/surface anchors vary under retained skins and are not approved target calibration
  values. No alias may be consumed until a named H4R-C/default/skin calibration passes.

### M1 — CONDITIONAL

- Noto consumers: **55 sites / 26 files**.
- Be Vietnam Pro is already loaded; M1 has no required dependency on M0R.
- Recommended scope: scoped VX-02 sans behavior in `app-demo/styles/typography.css`, adopted only by
  an explicitly approved consumer batch.
- No global h1/h2 rewrite and no Noto removal in M1.

### M2A — CONDITIONAL, SPLIT M2A0 → M2A1

Exact primitives: Opening Question, Guided Orientation, Clear Boundary and Consequence Before
Action. M2A0 creates the tested static safe shell; M2A1 adds guided state over it and rolls back
exactly to that retained shell. Helpers use Node-20-compatible `.mjs` plus `.d.mts`, with no new test
runner. Neither may import the current body taxonomy, recommendation or service ranking.

### M2B — CONDITIONAL AFTER M2A

- Structure: exact optional/collapse architecture is technically defined.
- Population: reviewed Service Scope, Visit/Process facts, learning destinations and Evidence Answer
  content remain deferred or blocked per fact.
- Zero evidence is a valid production state; no empty component is created.

### M3S

- Claim-bearing runtime surfaces found: **25** — 14 machine/metadata + 11 rendered groups.
- **M3S-1 machine-readable sanitation:** READY. Four JSON-LD emitter paths; safe omission PASS.
- **M3S-B booking false-success sanitation:** READY. Two exact modified paths; fail-closed no-send
  state; M4 remains blocked.
- **M3S-2 rendered/metadata sanitation:** CONDITIONAL / SPLIT REQUIRED. It cannot be approved as one
  broad batch.

### M3P — DEFERRED TO ADMIN

DE-01..04 remain content/reality debt after private opening. This is **not a UI-design blocker**.
Each future record remains fail-closed until relationship, provenance, permission/consent and
review are approved.

### M4 — BLOCKED

No endpoint/provider, receiving owner, data architecture, data-minimization/privacy/security,
timeout/retry/idempotency, handoff, monitoring or real confirmation evidence exists. Evidence
deferral does not unblock transport.

### M5 — DEFERRED

M5A homepage, M5B Noto, M5C tokens/skins/components and M5D Runtime Truth rebaseline require
successful corrected migration, verification and exact zero-consumer proof. No destructive cleanup
is eligible now.

## Readiness result

### Ready for human migration approval

1. `M3S-1` — omission of four unverified JSON-LD emitters.
2. `M3S-B` — removal of booking false-success behavior with a truthful local stopping state.

### Conditional

- `M0R` — immediate set 0; exact 13-candidate pool is conditional on a named calibrated consumer.
- `M1` — approve only with a named consumer or explicit dormant-contract reason.
- `M2A0` — reviewed structural copy + visual calibration + static browser verification.
- `M2A1` — after M2A0; guided state, exact sans contract and browser/state verification.
- `M2B` — after M2A; structure separate from factual population.
- `M3S-2` — split per rendered/metadata claim family with safe replacement/collapse.

### Deferred content

- `M3P` / DE-01..04 admin evidence population.
- reviewed Service Scope, Visit/Process and learning content that depends on DE-03/04.

### Blocked

- `M4` booking transport and real confirmed/handoff semantics.

### Deferred cleanup

- `M5A`, `M5B`, `M5C`, `M5D`.

## Recommended first H6 batch

**M3S-B — booking false-success sanitation.** It removes an active misleading consequence at the
point of user action, has two exact modified paths, makes no network request, preserves edit/
uncertain/stop control, and remains independent of the blocked M4 transport architecture. This
recommendation does not activate H6.

M3S-1 is also READY. M0R remains conditional because a no-op legacy binding is not an approved
target-value calibration.

## Adversarial pass A — target integrity

The independent reviewer found four target-integrity gaps. M0R promoted legacy/skin palette anchors
into an apparently approved semantic API, so it is now CONDITIONAL with a no-consumption lock until
named H4R-C/default/skin calibration. M2A could inherit global Noto serif, so M1 or an exact local
sans contract is now a hard predecessor with computed-font and forbidden-class checks. Public
FAILURE/RETRY specimens were removed from production M2A, and unequal-composition/mobile tests now
make Q/A metronome a failing condition. Re-run result: **PASS**. No archive-noun alias, gold target
role or target/runtime authority relapse remains.

## Adversarial pass B — evidence / content / trust

The independent reviewer found two evidence/trust gaps. CL-15 booking sanitation had been buried in
M3S-2 even though timer/iframe false success is independently unsafe. It is now exact READY batch
M3S-B; real transport and `CONFIRMED` semantics remain blocked in M4. Zero evidence also lacked a
deterministic stopping route. Artifacts 04–06 now require orientation → clear limit → local/not-sent
consequence → edit/remain uncertain/stop, while unavailable M2B branches return `null` with no CTA.
Re-run result: **PASS**; deferral grants no mock-fact permission and M3P remains non-blocking only
for structure.

## Adversarial pass C — engineering / reversibility

The independent reviewer found three material defects: seven metadata routes were missing, plain
Node tests could not import planned `.ts` helpers, and the M2A fallback was part of the same change
it claimed to remove. The inventory is now 25 surfaces with all seven paths; helpers use `.mjs` plus
`.d.mts`; and M2A is split into tested M2A0 static shell then M2A1 interaction. Lesser corrections
make M1 rollback consumer-aware, make M0R aliases just-in-time, scan all `app-demo` for JSON-LD,
and distinguish technical revert from safe fail-closed release recovery. Re-run result: **PASS**.
No dependency, backend, CMS, provider, deployment or hidden architecture mutation remains.

## Exit criteria

| Criterion                                                 | Result                                                     |
| --------------------------------------------------------- | ---------------------------------------------------------- |
| Gate B-R recorded                                         | PASS                                                       |
| Fresh runtime consumer graph                              | PASS                                                       |
| Corrected 41-role implementation strategy                 | PASS — 13-candidate pool; immediate set 0; M0R conditional |
| Exact Noto/Be Vietnam Pro map                             | PASS — 55/26; Be already loaded                            |
| Homepage current → target map                             | PASS                                                       |
| Structure vs factual population split                     | PASS                                                       |
| M3S inventory and safe omission                           | PASS — M3S-1 and M3S-B READY                               |
| M3P deferred, not design blocker                          | PASS                                                       |
| M4 blocked separately                                     | PASS                                                       |
| M5 deferred                                               | PASS                                                       |
| Exact paths, verification and rollback                    | PASS for READY; conditions explicit elsewhere              |
| Smallest useful H6 start                                  | PASS — M3S-B recommended                                   |
| Production/tokens/skins/DESIGN/dependencies/H4R unchanged | subject to final Git/hash verification                     |

## Gate consequence

```text
MIGRATION GATE-R PASS — PROPOSED
              |
              v
HUMAN MIGRATION APPROVAL
              |
     human names READY batch(es)?
        no ----------------------> H6 INACTIVE
        yes
         |
         v
ONLY THE NAMED FIRST BATCH ENTERS H6 DESIGN / CODE LOOP
```

Do not begin Migration or H6 automatically.
