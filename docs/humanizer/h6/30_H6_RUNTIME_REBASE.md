# 30 — H6 Runtime Rebase

## Status

**H6: PARTIAL — CROSS-PAGE MIGRATION DELTA REQUIRED.**

This rebase records only what runs after the implemented H6 batches. It does not promote locked
target truth, deferred evidence or blocked booking transport into runtime.

## Homepage runtime

```text
HomeHero (bounded copy; legacy red/gold + stock visual)
    ↓
OrientationCore
    ├─ ordinary-language choice / editable other / equal uncertainty
    ├─ exact user wording
    ├─ no diagnosis or hidden taxonomy
    └─ local no-send consequence + edit/remove/stop
    ↓
NarrativeCompletion
    ├─ bounded structural service categories
    ├─ expectation questions, not an operational process claim
    ├─ EvidenceAnswer → null (production approved records = 0)
    └─ local continuation to orientation
```

The old symptom → Tắc/Hàn/Hư/Loạn → recommendation graph is deleted. M2A0
`StaticOrientationShell` remains the rollback floor.

## Typography

- M1C is behavior-contract only; runtime class delta is zero.
- Corrected homepage uses Be Vietnam Pro sans-first component-local roles.
- Noto Serif remains loaded for live legacy display consumers in `HomeHero`, process and space.
- Noto count: 10 reference sites across 6 files; retirement is ineligible.
- No official brand font is claimed.

## Tokens and skins

- M0R-JIT alias count: zero.
- Legacy crimson/gold/wood tokens remain compatibility runtime, not Target Truth.
- retained skins: `yvien` default, `tet`, `an-tinh`;
- browser values differ as expected: default `#95131f/.42s`, tet `#b20000/.36s`, an-tinh
  `#2f5d50/.52s` for primary/motion anchors.

## Motion

- corrected homepage: native scroll and bounded CSS state/reveal motion;
- reduced-motion browser: fade and decorative leaf animation compute to `none`;
- legacy route motion and decorative composition remain coverage debt.

## Booking

- local three-step review only;
- forms 0, submit controls 0, iframes 0, mutating requests 0;
- no recipient, send, callback promise, success or confirmed state;
- unverified needs, service matching, price and duration remain, so booking is not H7 ready.

## Evidence and contact

- production evidence records/nodes: 0/0;
- no placeholder evidence, testimonial, staff metric or article body is reachable;
- contact route publishes no branch, channel, map or response promise;
- M3P: **DEFERRED TO ADMIN**;
- M4: **BLOCKED OPERATIONALLY**; no transport introduced.

## Coverage truth

H7-ready route families: `/lien-he`, `/tin-tuc`, `/tin-tuc/[slug]`.

H6-blocking debt: `/`, service index/details, booking, about, space, process, products, training,
partnership and all three public motion-lab routes. Eight families lack an `h1`. The P0 frontier is
production exposure of `/motion-lab*` specimen/dev controls.

## Reference firewall

The runtime visual system remains code/token-derived and exists without Sen or Triều. Sen affected
structure review only; Triều affected language/knowledge review only. Neither supplies Toplink
facts or visual identity. Firewall result: **PASS**.
