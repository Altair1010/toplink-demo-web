# 23 — M2B Narrative Graph

## Final homepage graph

```text
HomeHero                         current opening context; M3S-2 truth audit pending
    |
    v
OrientationCore                 ordinary wording / uncertainty / editable summary
    |
    v
ServiceScope                    category meaning only; no fit/outcome/price/duration
    |
    v
VisitProcessExplanation         expectation questions; no invented operating sequence
    |
    v
EvidenceAnswer                  production records = 0 --> true collapse --> DOM = 0
    |
    v
ContinueUnderstanding           truthful local continuation
    |
    `--> #hh-orientation-title  existing destination; no fake learning route
```

## Removed reachable jobs

```text
RitualTimeline -------------X mock visit sequence
SpaceAsTherapy -------------X unverified place claims + four equal frames
HomeFinalCTA ---------------X mock branch/contact + dominant conversion
```

## Composition jobs

```text
ORIENTATION     relational question + editable state
SERVICE SCOPE   ruled index
VISIT LIMIT     asymmetric expectation ledger
EVIDENCE        no record, no surface
CONTINUATION    open reading pause + one existing anchor
```

The sequence does not repeat one label/heading/paragraph/card shell. Service content uses indexed
rows, visit content uses a two-part expectation relation, evidence disappears entirely and
continuation uses a quiet downstream anchor.

## Reference firewall

Sen contributed only the relationship `orientation → service → process → contextual trust →
downstream action`. Triều contributed the distinction between understand/support, plain-language
bridging and the rule that reviewed provenance precedes knowledge publication. No palette, type,
visual identity, taxonomy, route tree or Toplink fact was copied from either corpus.
