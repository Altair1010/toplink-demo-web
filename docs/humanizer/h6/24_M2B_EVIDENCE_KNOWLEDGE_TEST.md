# 24 — M2B Evidence and Knowledge Test

## Evidence visibility

| Fixture           | Expected                                         | Result |
| ----------------- | ------------------------------------------------ | ------ |
| 0 records         | empty visible set; component returns `null`      | PASS   |
| 1 approved        | one exact question/answer/context                | PASS   |
| multiple approved | source order retained; no ranking/grid inference | PASS   |
| revoked           | hidden                                           | PASS   |
| unapproved        | hidden                                           | PASS   |

The pure Node evidence suite passes `4 / 4`. Production owns a literal empty array and the browser
DOM contains `0` `.hh-evidence-answer` surfaces. There is no empty frame, placeholder image, generic
quote, “coming soon” message or fabricated trust copy.

## Knowledge bridge

**DEFERRED / TRUE COLLAPSE.**

Fresh inspection found no reviewed Toplink learning destination. `content.ts` posts have no
Humanizer review/provenance state and contain mock authors/claims, so M2B does not link to `/tin-tuc`
or copy Triều IA/content. The only continuation link is the existing local
`#hh-orientation-title` anchor.

## Browser evidence

| Check                                | Result                                                     |
| ------------------------------------ | ---------------------------------------------------------- |
| zero-evidence production DOM         | `0` surfaces                                               |
| learning links                       | `[#hh-orientation-title]` only                             |
| legacy Ritual/Space/FinalCTA strings | `0 / 0 / 0`                                                |
| 375 / 768 / 1280 / 1440              | PASS; overflow `0`                                         |
| 200%-equivalent                      | PASS; overflow `0`                                         |
| heading order                        | H1 → orientation H2 → scope H2/H3 → visit H2 → continue H2 |
| reduced motion                       | PASS; all content visible                                  |
| mutating requests                    | `0`                                                        |
| unexpected console errors / warnings | `0 / 0`                                                    |

## Safety review

- no evidence population or admin path;
- no clinical suitability, outcome, price or duration;
- no literal operating sequence, staff/recipient or follow-up promise;
- no diagnostic or personalized YHCT explanation;
- no Triều route/taxonomy clone;
- no Sen visual transfer.
