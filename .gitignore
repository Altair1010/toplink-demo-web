node_modules
.env
.env.local
dist
build
.next
.DS_Store

# Internal-only — keep the public repo limited to the web demo
/.agents/
/presentation/
/Z-NeededUpdate/
/skills-lock.json
/.claude/
/.codegraph/
/.mcp.json
/CLAUDE.md
/bootstrap-project.ps1

# Công cụ extract token (clone riêng) & artifact kiểm thử — không đưa vào demo
/dembrandt/
/.playwright-mcp/
/.release/
/CLAUDE.docx

# Ảnh reference chụp màn hình ở ROOT — không phải asset của app
/*.jpeg
/*.jpg
/*.png

# Hệ agent nội bộ & script setup (giữ repo public gọn cho web demo)
# AGENTS.md CỐ Ý được track: nó là context chung cho mọi agent + mọi clone mới.
/.trellis/
/setup-claude-agent-system.ps1